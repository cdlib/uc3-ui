<nav class="c-breadcrumb" aria-label="breadcrumb">
  <ul>
  {{#each breadcrumb.text}}
    {{!-- if the last array item --}}
    {{#if @last}}
    <li>{{this}}</li>
    {{else}}
    <li><a href="">{{this}}</a></li>
    {{/if}}
  {{/each}}
  </ul>
</nav>
