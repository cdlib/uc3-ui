<section class="c-bio">
  <header>
    <h2>{{ bio.name }}</h2>
    <a href="mailto:{{ bio.email }}">{{ bio.email }}</a>
  </header>
  <img class="c-bio__image" src="{{ bio.image }}" alt="">
  <div class="c-bio__caption">
    <p>{{{ bio.paragraphs }}}</h2></p>
  </div>
</section>
